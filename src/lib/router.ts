/** Class that will handle every route/path interaction/event. */
export class Router {
  /** TODO */
  private pathChangesSetup() {

  }

  /**
   * @param path URL entered by the user.
   */
  private onPathChanged(path: string) {

  }

  // window.onpopstate = () => {
  //   contentDiv.innerHTML = routes[window.location.pathname];
  // }
}
